.Rproj.user
.Rhistory
.RData
node_modules/
test-results/
web/webr/
# web/R/ is a build-time copy of the top-level R/ sources; never commit it.
web/R/
*.DS_Store
.gstack/
# Machine-local agent session state.
.claude/
# Scraped third-party sources (journal pages) kept for research provenance —
# never publish: the captures are someone else's copyrighted content.
.firecrawl/
