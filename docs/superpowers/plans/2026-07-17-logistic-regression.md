# Logistic Regression (binary-outcome Table 3) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a sixth guided analysis — univariable + multivariable **logistic regression** producing a clinical "Table 3" (unadjusted OR beside adjusted OR), an adjusted-OR forest plot, model-quality guards (separation detection, EPV advisory, C-statistic, VIF, influential-point flag), and a downloadable R script — mirroring the existing Cox module.

**Architecture:** A binary-outcome Table 3 is structurally identical to the Cox HR table with three substitutions: `coxph`→`glm(family=binomial)`, HR→OR (`exp(coef)`), PH-check→(non-blocking) separation/EPV/discrimination guards. We clone `R/cox.R`→`R/logistic.R` and `web/guided/cox/`→`web/guided/logistic/`, adapting for a binary outcome (event-value picker on the *outcome* column, not a survival status), continuous-covariate **per-increment rescaling**, and base-R diagnostics. Everything runs on the boot package set (`ggplot2`, `svglite`, `jsonlite`) plus base `stats` — **no `EXTRA_PACKAGES` entry, no new `DESCRIPTION` import**.

**Tech Stack:** R (base `stats::glm`, `ggplot2`, `svglite`) authored+tested as the `manuscriptfigures` package; webR (WASM) delivery; vanilla-JS guided shell (`web/guided/shell.js`); testthat (R), plain-Node (JS unit), Playwright (e2e).

## Global Constraints

- **No data egress.** Only network calls are the webR runtime/packages from CDN and `fetch("R/*.R")`. No analytics, no upload. Only mapped columns cross into the spec.
- **No new R package.** `fig_logistic` uses base `stats` (`glm`, `lm`, `cooks.distance`, `rank`) + `ggplot2` + `svglite` only. Do **not** add `broom`, `broom.helpers`, `pROC`, `logistf`, `car`, or `brglm2` to `DESCRIPTION` or `worker.js` `EXTRA_PACKAGES`. (Firth/`brglm2` and Box-Tidwell linearity are explicitly deferred to a fast-follow.)
- **WARN 0 is a hard gate.** Run the R suite with `Rscript -e 'devtools::test()'` (never `testthat::test_file`). `[ FAIL 0 | WARN 0 | ... ]` required. ggplot2 4.x: use `linewidth` not `size`/`label.size`; `geom_errorbar(orientation="y")` not `geom_errorbarh`. Wrap only a library-internal call in `suppressWarnings()` — never your own model fitting.
- **Figure key is `logistic`** everywhere (dispatch switch, `data-figure` button, form registry, worker fetch list). R function is `fig_logistic`. Nav label is exactly `Logistic regression`.
- **Five parallel keys must stay in sync** or it 404s/mis-routes: `R/logistic.R`, `R/dispatch.R` switch, `web/worker.js` fetch loop, `web/app.js` registry + `web/index.html` button, and (for this analysis, no new package). See CLAUDE.md "Adding a figure or analysis".
- **`web/R/` is a gitignored build copy.** Before any e2e/serve: `rm -rf web/R && cp -R R web/R`.
- **CIs are Wald** (`exp(est ± 1.96·se)` from `summary(fit)$coefficients`), not profile-likelihood — faster in webR and stable under separation. This is the manuscript-standard OR interval.
- **Reliability rule:** an OR cell reads `"not reliably estimated"` when the estimate/CI is non-finite or `exp(upper) > 1e6` (the separation signature).

---

## File Structure

**Create:**
- `R/logistic.R` — `fig_logistic(spec)` + helpers (`.logistic_*`), returning `list(svg=, text=, code=)`.
- `tests/testthat/test-logistic.R` — R unit tests.
- `data-raw/logistic-demo-generator.R` — frozen synthetic demo generator (writes JS + fixture CSV).
- `web/guided/logistic/demo-data.js` — GENERATED by the demo generator (do not hand-edit).
- `web/guided/logistic/spec.js` — pure spec assembly (`buildLogisticSpec`, `distinctValues`, `mostFrequent`).
- `web/guided/logistic/spec.test.mjs` — JS unit test for spec assembly.
- `web/guided/logistic/analyze-form.js` — upload UI (`renderLogisticAnalyzeForm`).
- `web/guided/logistic/content.js` — Understand sections + example intro + experiment controls.
- `web/guided/logistic/demo.js` — demo table + default state + `buildLogisticDemoSpec`.
- `web/guided/logistic/guided-logistic.js` — `createGuidedShell({...})` config → `renderGuidedLogistic`.
- `tests/e2e/logistic-guided.spec.js` — Playwright e2e.

**Modify:**
- `R/dispatch.R:8-14` — add `logistic = fig_logistic(spec),` to the `switch`.
- `web/worker.js:42` — add `"logistic.R"` to the boot fetch loop. (No `EXTRA_PACKAGES` entry.)
- `web/app.js:107-111` — import + register `logistic: renderGuidedLogistic`.
- `web/index.html:~45` — add `<button data-figure="logistic">` after the Cox button.
- `web/sw.js` (service worker) — bump the cache name so the new nav item isn't served stale.
- `tests/e2e/smoke.spec.js` — six analyses instead of five.
- `CLAUDE.md` — reflect logistic as the sixth guided analysis.
- `MEMORY.md` + a new memory file — session note.

---

## Task 1: R core — prep, fits, OR table, methods text

**Files:**
- Create: `R/logistic.R`
- Create: `tests/testthat/test-logistic.R`

**Interfaces:**
- Consumes: `%||%`, `.svg_string` (`R/dispatch.R`); `.esc`, `.numeric_col`, `.char_col` (`R/summarize.R`); `.km_palette` (`R/km.R`); `.fig_theme` (`R/themes.R`); `.script_assemble`, `.script_dep` (`R/script.R`). All already on the search path when the package is loaded.
- Produces: `fig_logistic(spec) -> list(svg=<character>, text=<character>, code=<character>)`. `spec$figure=="logistic"`; `spec$roles$outcome` (single column), `spec$roles$covariates` (list); `spec$options$event_value` (which outcome value = 1), `spec$options$ref_levels` (named list, per categorical covariate), `spec$options$increments` (named list, per numeric covariate, default 1). Helpers produced for later tasks: `.logistic_prep(spec)`, `.logistic_fits(df, covs, cov_types)`, `.logistic_rows(p, fits)`, `.logistic_table_html(disp_rows)`.

- [ ] **Step 1: Write the failing test**

Create `tests/testthat/test-logistic.R`:

```r
# Binary-outcome dataset with a real arm effect confounded by age. New treatment
# is channeled to older patients (higher risk), so its UNADJUSTED odds ratio looks
# weak and only adjusting for age reveals the protective effect (~exp(-0.9)=0.41).
mk_logit_rows <- function() {
  set.seed(52)
  n <- 240
  arm <- rep(c("Control", "Treated"), each = n / 2)
  age <- round(rnorm(n, 60, 10) + 6 * (arm == "Treated"), 1)
  lp <- -1 + 0.06 * (age - 60) - 0.9 * (arm == "Treated")
  y <- rbinom(n, 1, plogis(lp))
  outcome <- ifelse(y == 1, "Event", "NoEvent")
  lapply(seq_len(n), function(i)
    list(outcome = outcome[i], arm = arm[i], age = age[i]))
}
sc_logit <- function(rows, covariates = c("arm", "age"), ref_levels = NULL,
                     increments = NULL, event_value = "Event") {
  list(figure = "logistic", data = rows,
       roles = list(outcome = "outcome", covariates = as.list(covariates)),
       options = list(event_value = event_value,
                      ref_levels = ref_levels %||% list(),
                      increments = increments %||% list()))
}

test_that("fig_logistic returns svg table, text, and code", {
  out <- fig_logistic(sc_logit(mk_logit_rows()))
  expect_match(out$svg, "<table", fixed = TRUE)
  expect_true(nzchar(out$text))
  expect_true(nzchar(out$code))
})

test_that("both unadjusted and adjusted ORs are reported", {
  out <- fig_logistic(sc_logit(mk_logit_rows()))
  expect_match(out$text, "adjusted", ignore.case = TRUE)
  expect_match(out$text, "[Uu]nadjusted")
})

test_that("adjusted arm OR recovers the simulated protective effect (< 1)", {
  out <- fig_logistic(sc_logit(mk_logit_rows()))
  or <- as.numeric(sub(".*Treated[^0-9]*([0-9.]+).*", "\\1",
                       gsub("\n", " ", out$text)))
  expect_true(or < 0.85)
})

test_that("reference level appears and can be overridden", {
  out <- fig_logistic(sc_logit(mk_logit_rows()))
  expect_match(out$svg, "reference", ignore.case = TRUE)
  out2 <- fig_logistic(sc_logit(mk_logit_rows(), ref_levels = list(arm = "Treated")))
  expect_match(out2$svg, "Treated", fixed = TRUE)
})

test_that("no covariate selected errors readably", {
  expect_error(fig_logistic(sc_logit(mk_logit_rows(), covariates = character(0))),
               "covariate", ignore.case = TRUE)
})

test_that("too few events errors readably", {
  rows <- mk_logit_rows()[1:60]
  rows <- lapply(rows, function(r) { r$outcome <- "NoEvent"; r })
  rows[[1]]$outcome <- "Event"; rows[[2]]$outcome <- "Event"
  expect_error(fig_logistic(sc_logit(rows)), "event", ignore.case = TRUE)
})

test_that("constant covariate errors readably", {
  rows <- lapply(mk_logit_rows(), function(r) { r$arm <- "Only"; r })
  expect_error(fig_logistic(sc_logit(rows, covariates = c("arm"))),
               "one level", ignore.case = TRUE)
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `Rscript -e 'devtools::test(filter = "logistic")'`
Expected: FAIL — `could not find function "fig_logistic"`.

- [ ] **Step 3: Write minimal implementation**

Create `R/logistic.R` (mirrors `R/cox.R` prep/fit/rows/table; Wald CIs; binary outcome recode):

```r
# R/logistic.R
# Binary-outcome logistic regression: univariable (unadjusted) odds ratio per
# covariate beside the joint-model adjusted OR (clinical "Table 3"), an
# adjusted-OR forest plot, and non-blocking model-quality guards (separation,
# EPV, C-statistic, VIF, influential points). Base stats::glm only — no extra
# package. Wald CIs: exp(est +/- 1.96 se), stable under separation.

# TRUE iff every non-blank cell of a column parses as numeric (mirrors .cox_is_numeric).
.logistic_is_numeric <- function(rows, colname) {
  raw <- .char_col(rows, colname)
  raw[!is.na(raw) & raw == ""] <- NA
  num <- suppressWarnings(as.numeric(raw))
  !any(!is.na(raw) & is.na(num))
}

# Most frequent non-NA value of a character vector (default reference level).
.logistic_most_frequent <- function(x) {
  x <- x[!is.na(x) & x != ""]
  names(sort(table(x), decreasing = TRUE))[1]
}

# Per-covariate positive increment (default 1); non-numeric/invalid -> 1.
.logistic_increment <- function(increments, cl) {
  k <- suppressWarnings(as.numeric(increments[[cl]] %||% 1))
  if (length(k) != 1 || is.na(k) || !is.finite(k) || k <= 0) 1 else k
}

# Build the complete-case working frame and covariate metadata.
.logistic_prep <- function(spec) {
  rows <- spec$data
  if (is.null(rows) || length(rows) == 0) stop("No data rows provided.")
  covs <- unlist(spec$roles$covariates %||% list())
  if (length(covs) == 0) stop("Select at least one covariate.")
  ocol <- spec$roles$outcome
  if (is.null(ocol)) stop("Choose an outcome column.")
  have <- names(rows[[1]])
  for (cl in c(ocol, covs))
    if (!(cl %in% have)) stop(sprintf("Column '%s' not found in the data.", cl))

  ev <- as.character(spec$options$event_value %||% "")
  outcome_raw <- .char_col(rows, ocol)
  ev_num <- suppressWarnings(as.numeric(ev))
  y <- as.integer(outcome_raw == ev |
    (!is.na(ev_num) & !is.na(suppressWarnings(as.numeric(outcome_raw))) &
       suppressWarnings(as.numeric(outcome_raw)) == ev_num))
  y[is.na(y)] <- NA_integer_

  cov_types <- setNames(
    vapply(covs, function(cl) if (.logistic_is_numeric(rows, cl)) "numeric" else "categorical",
           character(1)), covs)
  ref_levels <- spec$options$ref_levels %||% list()
  increments <- spec$options$increments %||% list()
  incr <- setNames(vapply(covs, function(cl)
    if (cov_types[[cl]] == "numeric") .logistic_increment(increments, cl) else NA_real_,
    numeric(1)), covs)

  cols <- list(.y = y)
  for (cl in covs) {
    if (cov_types[[cl]] == "numeric") cols[[cl]] <- .numeric_col(rows, cl)
    else { v <- .char_col(rows, cl); v[!is.na(v) & v == ""] <- NA; cols[[cl]] <- v }
  }
  df <- as.data.frame(cols, stringsAsFactors = FALSE, check.names = FALSE)
  n_before <- nrow(df)
  df <- df[stats::complete.cases(df), , drop = FALSE]
  n_dropped <- n_before - nrow(df)

  # Rescale numeric covariates to the chosen increment (per-k OR = exp(k*beta));
  # this changes OR and CI but NOT the p-value (Wald statistic is scale-invariant).
  for (cl in covs) if (cov_types[[cl]] == "numeric" && incr[[cl]] != 1)
    df[[cl]] <- df[[cl]] / incr[[cl]]

  # Factor + relevel categoricals; default reference = most frequent level.
  for (cl in covs) if (cov_types[[cl]] == "categorical") {
    ref <- as.character(ref_levels[[cl]] %||% .logistic_most_frequent(df[[cl]]))
    lv <- unique(df[[cl]])
    if (length(lv) < 2) stop(sprintf("Covariate '%s' has only one level after removing missing values.", cl))
    if (!ref %in% lv) ref <- .logistic_most_frequent(df[[cl]])
    df[[cl]] <- stats::relevel(factor(df[[cl]]), ref = ref)
  }
  n_event <- sum(df$.y == 1)
  n_min <- min(n_event, nrow(df) - n_event)
  if (n_min < 10) stop(sprintf("Too few events (%d in the smaller outcome group) to fit a reliable logistic model; at least 10 are needed.", n_min))
  list(df = df, covs = covs, cov_types = cov_types, ref_levels = ref_levels,
       incr = incr, n = nrow(df), n_event = n_event, n_min = n_min, n_dropped = n_dropped)
}

# Fit one glm, capturing (not suppressing) fit warnings so an unreliable OR can be
# flagged. muffleWarning keeps it off the WARN-0 gate. The "fitted probabilities
# numerically 0 or 1" warning is the separation signal, read in fig_logistic.
.logistic_fit_one <- function(formula, df) {
  warn <- NULL
  fit <- withCallingHandlers(
    stats::glm(formula, data = df, family = stats::binomial()),
    warning = function(w) { warn <<- conditionMessage(w); invokeRestart("muffleWarning") })
  list(fit = fit, warn = warn)
}

# Univariable glm per covariate + one joint model.
.logistic_fits <- function(df, covs, cov_types) {
  uni <- lapply(covs, function(cl)
    .logistic_fit_one(stats::as.formula(sprintf(".y ~ `%s`", cl)), df))
  names(uni) <- covs
  joint_f <- stats::as.formula(paste0(".y ~ ",
    paste(sprintf("`%s`", covs), collapse = " + ")))
  joint <- .logistic_fit_one(joint_f, df)
  list(uni = uni, joint = joint)
}

# Wald OR (95% CI, p) for one coefficient key from a summary() matrix. Returns
# "not reliably estimated" when non-finite or the CI blows up past OR 1e6
# (the separation signature).
.logistic_or_cell <- function(smat, key) {
  if (!(key %in% rownames(smat))) return("not reliably estimated")
  est <- smat[key, "Estimate"]; se <- smat[key, "Std. Error"]
  p <- smat[key, "Pr(>|z|)"]
  or <- exp(est); lo <- exp(est - 1.96 * se); hi <- exp(est + 1.96 * se)
  if (!is.finite(or) || !is.finite(lo) || !is.finite(hi) || hi > 1e6)
    return("not reliably estimated")
  pf <- if (p < 0.001) "p<0.001" else sprintf("p=%.3f", p)
  sprintf("%.2f (%.2f–%.2f, %s)", or, lo, hi, pf)
}

# One display row per covariate LEVEL (reference marked). Numeric rows label the
# increment. `term` leading with two spaces marks an indented level row.
.logistic_rows <- function(p, fits) {
  rows <- list()
  jsm <- summary(fits$joint$fit)$coefficients
  for (cl in p$covs) {
    usm <- summary(fits$uni[[cl]]$fit)$coefficients
    if (p$cov_types[[cl]] == "numeric") {
      k <- p$incr[[cl]]
      unit <- if (k == 1) "per 1 unit" else sprintf("per %g units", k)
      rows[[length(rows) + 1]] <- list(term = sprintf("%s (%s)", cl, unit),
        unadj = .logistic_or_cell(usm, cl), adj = .logistic_or_cell(jsm, cl))
    } else {
      lv <- levels(p$df[[cl]])
      rows[[length(rows) + 1]] <- list(term = sprintf("%s (reference: %s)", cl, lv[1]),
        unadj = "", adj = "")
      for (l in lv[-1]) {
        key <- paste0(cl, l)
        rows[[length(rows) + 1]] <- list(term = paste0("  ", l),
          unadj = .logistic_or_cell(usm, key), adj = .logistic_or_cell(jsm, key))
      }
    }
  }
  rows
}

# HTML table (reuses .esc). Mirrors .cox_table_html: "(reference:" header rows keep
# blank effect cells; other blank cells read "1 (reference)".
.logistic_table_html <- function(disp_rows) {
  header <- "<tr><th>Characteristic</th><th>Unadjusted OR (95% CI)</th><th>Adjusted OR (95% CI)</th></tr>"
  body <- vapply(disp_rows, function(r) {
    is_header <- grepl("\\(reference:", r$term)
    indent <- startsWith(r$term, "  ")
    label <- .esc(trimws(r$term))
    if (indent) label <- paste0("<span class=\"lvl\">", label, "</span>")
    if (is_header) { unadj <- ""; adj <- "" }
    else {
      unadj <- if (nzchar(r$unadj)) .esc(r$unadj) else "1 (reference)"
      adj <- if (nzchar(r$adj)) .esc(r$adj) else "1 (reference)"
    }
    sprintf("<tr><td>%s</td><td>%s</td><td>%s</td></tr>", label, unadj, adj)
  }, character(1))
  paste0("<table class=\"table1\"><thead>", header, "</thead><tbody>",
         paste(body, collapse = ""), "</tbody></table>")
}

fig_logistic <- function(spec) {
  p <- .logistic_prep(spec)
  fits <- .logistic_fits(p$df, p$covs, p$cov_types)
  disp_rows <- .logistic_rows(p, fits)

  table_html <- .logistic_table_html(disp_rows)
  svg_field <- sprintf("<div class=\"summary-output\"><div class=\"table-scroll\">%s</div></div>",
                       table_html)

  tsv <- paste(c(paste(c("Characteristic", "Unadjusted OR (95% CI, p)",
                         "Adjusted OR (95% CI, p)"), collapse = "\t"),
                 vapply(disp_rows, function(r)
                   paste(c(trimws(r$term), r$unadj, r$adj), collapse = "\t"), character(1))),
               collapse = "\n")
  drop_note <- if (p$n_dropped > 0)
    sprintf(" %d row(s) with missing values were excluded.", p$n_dropped) else ""
  methods <- sprintf(paste0("Multivariable logistic regression (n = %d, %d events) ",
    "adjusted for %s. Unadjusted odds ratios are from single-covariate models; ",
    "adjusted odds ratios are from the joint model.%s"),
    p$n, p$n_event, paste(p$covs, collapse = ", "), drop_note)
  text <- paste0(tsv, "\n\n", methods)

  list(svg = svg_field, text = text, code = "")
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `Rscript -e 'devtools::test(filter = "logistic")'`
Expected: PASS, `[ FAIL 0 | WARN 0 | ... ]`. (The `code=""` placeholder satisfies `nzchar(out$code)`? No — it does not.) **Change the Step-1 `code` assertion** temporarily is NOT allowed; instead the `code` field is filled in Task 4. For Task 1, relax only the first test: replace `expect_true(nzchar(out$code))` with `expect_true(!is.null(out$code))`. Task 4 restores the `nzchar` assertion.

- [ ] **Step 5: Commit**

```bash
rm -rf web/R && cp -R R web/R
git add R/logistic.R tests/testthat/test-logistic.R
git commit -m "feat(logistic): pure OR Table 3 core + R unit tests (glm, Wald CI, no extra pkg)"
```

---

## Task 2: Adjusted-OR forest plot

**Files:**
- Modify: `R/logistic.R` (add `.logistic_forest_svg`, wire into `svg_field`)
- Modify: `tests/testthat/test-logistic.R` (add forest tests)

**Interfaces:**
- Consumes: `.km_palette`, `.fig_theme`, `.svg_string`; `p` and `fits` from Task 1.
- Produces: `.logistic_forest_svg(p, fits) -> <character>` (an `<svg>` or `""`). `fig_logistic`'s `svg` now contains a `<table>` followed by an `<svg>`.

- [ ] **Step 1: Write the failing test**

Append to `tests/testthat/test-logistic.R`:

```r
test_that("fig_logistic svg includes a forest-plot svg", {
  out <- fig_logistic(sc_logit(mk_logit_rows()))
  expect_match(out$svg, "<svg", fixed = TRUE)
  expect_gt(nchar(out$svg), 800)
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `Rscript -e 'devtools::test(filter = "logistic")'`
Expected: FAIL — `out$svg` has no `<svg>` yet.

- [ ] **Step 3: Write minimal implementation**

Add to `R/logistic.R` (mirrors `.cox_forest_svg`; null line at OR=1, log axis):

```r
# Forest plot of ADJUSTED ORs (one point per non-reference level / numeric term),
# log x-axis, dashed rule at OR = 1. geom_errorbar(orientation="y") — never
# geom_errorbarh; linewidth — never size.
.logistic_forest_svg <- function(p, fits) {
  sm <- summary(fits$joint$fit)$coefficients
  keys <- setdiff(rownames(sm), "(Intercept)")
  est <- sm[keys, "Estimate"]; se <- sm[keys, "Std. Error"]
  or <- exp(est); lo <- exp(est - 1.96 * se); hi <- exp(est + 1.96 * se)
  keep <- is.finite(or) & is.finite(lo) & is.finite(hi) & hi <= 1e6
  if (!any(keep)) return("")
  labeller <- function(key) {
    for (cl in p$covs) if (startsWith(key, cl)) {
      if (p$cov_types[[cl]] == "numeric") {
        k <- p$incr[[cl]]
        return(if (k == 1) sprintf("%s (per 1 unit)", cl) else sprintf("%s (per %g units)", cl, k))
      }
      return(sprintf("%s: %s", cl, sub(paste0("^", cl), "", key)))
    }
    key
  }
  d <- data.frame(term = vapply(keys[keep], labeller, character(1)),
                  or = or[keep], lo = lo[keep], hi = hi[keep],
                  stringsAsFactors = FALSE)
  d$term <- factor(d$term, levels = rev(d$term))
  pal <- .km_palette(nrow(d))
  gg <- ggplot2::ggplot(d, ggplot2::aes(x = or, y = term, color = term)) +
    ggplot2::geom_vline(xintercept = 1, linetype = "dashed", linewidth = 0.5,
                        colour = "grey50") +
    ggplot2::geom_errorbar(ggplot2::aes(xmin = lo, xmax = hi), orientation = "y",
                           width = 0.2, linewidth = 0.6) +
    ggplot2::geom_point(size = 2.4) +
    ggplot2::scale_x_log10() +
    ggplot2::scale_color_manual(values = pal, guide = "none") +
    ggplot2::labs(x = "Adjusted odds ratio (log scale)", y = NULL) +
    .fig_theme("generic")
  .svg_string(gg, width = 6, height = 0.5 + 0.5 * nrow(d))
}
```

In `fig_logistic`, replace the `svg_field` line with:

```r
  forest_svg <- .logistic_forest_svg(p, fits)
  svg_field <- sprintf("<div class=\"summary-output\"><div class=\"table-scroll\">%s</div>%s</div>",
                       table_html, forest_svg)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `Rscript -e 'devtools::test(filter = "logistic")'`
Expected: PASS, `[ FAIL 0 | WARN 0 | ... ]`.

- [ ] **Step 5: Commit**

```bash
rm -rf web/R && cp -R R web/R
git add R/logistic.R tests/testthat/test-logistic.R
git commit -m "feat(logistic): adjusted-OR forest plot (log axis, null line at OR=1)"
```

---

## Task 3: Model-quality guards — separation, EPV, C-statistic, VIF, influential points

**Files:**
- Modify: `R/logistic.R` (`.logistic_auc`, `.logistic_vif`, guard lines in `fig_logistic`)
- Modify: `tests/testthat/test-logistic.R`

**Interfaces:**
- Consumes: `p`, `fits`; base `stats::lm`, `stats::cooks.distance`, `rank`.
- Produces: `.logistic_auc(prob, y) -> numeric(1)`; `.logistic_vif(df, num_covs) -> named numeric | NULL`. `fig_logistic` methods text gains up to five non-blocking sentences (separation CAUTION, EPV CAUTION, C-statistic, VIF CAUTION, influential-point note).

- [ ] **Step 1: Write the failing test**

Append to `tests/testthat/test-logistic.R`:

```r
test_that("methods text reports the C-statistic (discrimination)", {
  out <- fig_logistic(sc_logit(mk_logit_rows()))
  expect_match(out$text, "C-statistic", fixed = TRUE)
})

test_that("EPV warning fires when events per term is under 10", {
  # 12 covariate levels' worth of terms vs ~10 events -> EPV < 10.
  set.seed(9); n <- 60
  g <- sample(LETTERS[1:6], n, replace = TRUE)   # 5 non-reference terms
  x <- rnorm(n)
  y <- ifelse(seq_len(n) <= 10, "Event", "NoEvent")
  rows <- lapply(seq_len(n), function(i) list(outcome = y[i], g = g[i], x = x[i]))
  out <- fig_logistic(sc_logit(rows, covariates = c("g", "x")))
  expect_match(out$text, "EPV", fixed = TRUE)
})

test_that("separation is detected and flagged, not silently shipped", {
  # `flag` perfectly predicts the outcome -> quasi-complete separation.
  set.seed(3); n <- 120
  y <- rbinom(n, 1, 0.5)
  flag <- ifelse(y == 1, "hi", "lo")           # perfect predictor
  age <- rnorm(n, 60, 10)
  rows <- lapply(seq_len(n), function(i)
    list(outcome = ifelse(y[i] == 1, "Event", "NoEvent"), flag = flag[i], age = age[i]))
  out <- fig_logistic(sc_logit(rows, covariates = c("flag", "age")))
  expect_match(out$text, "separation", ignore.case = TRUE)
  expect_match(out$svg, "not reliably estimated", fixed = TRUE)
})

test_that("multicollinearity (VIF) is flagged for near-duplicate covariates", {
  set.seed(7); n <- 300
  x1 <- rnorm(n)
  x2 <- x1 + rnorm(n, 0, 0.05)                 # r ~ 0.99 with x1
  y <- rbinom(n, 1, plogis(0.5 * x1))
  rows <- lapply(seq_len(n), function(i)
    list(outcome = ifelse(y[i] == 1, "Event", "NoEvent"), x1 = x1[i], x2 = x2[i]))
  out <- fig_logistic(sc_logit(rows, covariates = c("x1", "x2")))
  expect_match(out$text, "VIF", fixed = TRUE)
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `Rscript -e 'devtools::test(filter = "logistic")'`
Expected: FAIL — no `C-statistic`/`EPV`/`separation`/`VIF` text yet.

- [ ] **Step 3: Write minimal implementation**

Add helpers to `R/logistic.R`:

```r
# C-statistic (AUC) == normalized Mann-Whitney U from fitted probabilities.
# Base stats only; rank() midranks handle ties. NA if a class is empty.
.logistic_auc <- function(prob, y) {
  n1 <- sum(y == 1); n0 <- sum(y == 0)
  if (n1 == 0 || n0 == 0) return(NA_real_)
  (sum(rank(prob)[y == 1]) - n1 * (n1 + 1) / 2) / (n1 * n0)
}

# VIF per CONTINUOUS predictor via base lm: VIF_j = 1/(1 - R^2_j), R^2_j from
# regressing predictor j on the other continuous predictors. Valid only for
# single-df (continuous) terms — categorical collinearity is not assessed. NULL
# when fewer than 2 continuous covariates.
.logistic_vif <- function(df, num_covs) {
  if (length(num_covs) < 2) return(NULL)
  setNames(vapply(num_covs, function(v) {
    others <- setdiff(num_covs, v)
    f <- stats::as.formula(sprintf("`%s` ~ %s", v,
      paste(sprintf("`%s`", others), collapse = " + ")))
    r2 <- summary(stats::lm(f, data = df))$r.squared
    if (!is.finite(r2) || r2 >= 1) Inf else 1 / (1 - r2)
  }, numeric(1)), num_covs)
}
```

In `fig_logistic`, after `disp_rows <- .logistic_rows(p, fits)` and before building `svg_field`, insert the guard block, then append the lines to `methods`:

```r
  jfit <- fits$joint$fit

  # Separation (non-blocking): glm's "fitted probabilities 0/1" warning, or any
  # adjusted OR that blew past the reliability threshold.
  jsm <- summary(jfit)$coefficients
  sep_warn <- !is.null(fits$joint$warn) && grepl("fitted probabilities", fits$joint$warn)
  sep_cell <- any(vapply(disp_rows, function(r) identical(r$adj, "not reliably estimated"), logical(1)))
  sep_line <- if (sep_warn || sep_cell)
    paste0(" CAUTION: separation was detected — one or more covariates predict the outcome ",
           "(near-)perfectly, so those odds ratios are not reliably estimated by standard ",
           "logistic regression. Consider collapsing sparse categories, dropping the term, ",
           "or a penalized (Firth) fit, and seek statistical review.") else ""

  # EPV (non-blocking soft advisory; the '10' threshold is a heuristic, not a gate).
  n_terms <- sum(vapply(p$covs, function(cl)
    if (p$cov_types[[cl]] == "numeric") 1L else nlevels(p$df[[cl]]) - 1L, integer(1)))
  epv <- p$n_min / n_terms
  epv_line <- if (epv < 10)
    sprintf(paste0(" CAUTION: about %.1f events per model term (EPV < 10); the adjusted ",
                   "estimates may be unstable and are best treated as exploratory."), epv) else ""

  # Discrimination: overall C-statistic (base-R AUC). Not per-covariate.
  auc <- .logistic_auc(stats::fitted(jfit), p$df$.y)
  auc_line <- if (is.finite(auc))
    sprintf(" Overall model discrimination: C-statistic = %.2f.", auc) else ""

  # Multicollinearity: VIF for continuous predictors only.
  num_covs <- names(p$cov_types)[p$cov_types == "numeric"]
  vif <- .logistic_vif(p$df, num_covs)
  vif_line <- if (!is.null(vif) && any(vif > 5))
    sprintf(paste0(" CAUTION: multicollinearity among continuous covariates ",
                   "(largest VIF = %.1f > 5); consider dropping a redundant variable."),
            max(vif[is.finite(vif)], na.rm = TRUE)) else ""

  # Influential observations (non-blocking): Cook's distance > 4/n.
  cd <- stats::cooks.distance(jfit)
  n_infl <- sum(cd > 4 / length(cd), na.rm = TRUE)
  infl_line <- if (n_infl > 0)
    sprintf(" %d observation(s) were flagged as influential (Cook's distance > 4/n); inspect for data-entry errors.", n_infl) else ""
```

Then change the `methods <- sprintf(...)` call so its format string ends with `%s%s%s%s%s%s` and appends `sep_line, epv_line, auc_line, vif_line, infl_line, drop_note` (drop_note last):

```r
  methods <- sprintf(paste0("Multivariable logistic regression (n = %d, %d events) ",
    "adjusted for %s. Unadjusted odds ratios are from single-covariate models; ",
    "adjusted odds ratios are from the joint model.%s%s%s%s%s%s"),
    p$n, p$n_event, paste(p$covs, collapse = ", "),
    sep_line, epv_line, auc_line, vif_line, infl_line, drop_note)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `Rscript -e 'devtools::test(filter = "logistic")'`
Expected: PASS, `[ FAIL 0 | WARN 0 | ... ]`.

- [ ] **Step 5: Commit**

```bash
rm -rf web/R && cp -R R web/R
git add R/logistic.R tests/testthat/test-logistic.R
git commit -m "feat(logistic): non-blocking guards — separation, EPV, C-statistic, VIF, Cook's D"
```

---

## Task 4: Downloadable R script (`code` field)

**Files:**
- Modify: `R/logistic.R` (add `.logistic_script`, return it as `code`)
- Modify: `tests/testthat/test-logistic.R`

**Interfaces:**
- Consumes: `.script_assemble` (`R/script.R`), `spec`, `p`, `fits`.
- Produces: `.logistic_script(spec, p, fits) -> <character>` — a complete runnable R script (embedded example data for demo specs; `read.csv` for uploads via `source_roles`). `fig_logistic` returns it as `code`.

- [ ] **Step 1: Write the failing test**

First restore the real assertion from Task 1 Step 4: in the first test change `expect_true(!is.null(out$code))` back to `expect_true(nzchar(out$code))`. Then append:

```r
test_that("logistic code parses as R and mentions glm(binomial)", {
  out <- fig_logistic(sc_logit(mk_logit_rows()))
  expect_silent(parse(text = out$code))
  expect_match(out$code, "glm(", fixed = TRUE)
  expect_match(out$code, "binomial", fixed = TRUE)
})

test_that("demo-shape spec embeds data (no read.csv) in the script", {
  out <- fig_logistic(sc_logit(mk_logit_rows()))   # no source_filename -> embedded
  expect_match(out$code, "data.frame", fixed = TRUE)
  expect_false(grepl("read.csv", out$code, fixed = TRUE))
})

test_that("upload-shape spec reads the user's real column names", {
  spec <- sc_logit(mk_logit_rows())
  spec$options$source_filename <- "mydata.csv"
  spec$options$source_roles <- list(outcome = "outcome",
    covariates = as.list(c("arm", "age")), event = "Event")
  out <- fig_logistic(spec)
  expect_match(out$code, "read.csv", fixed = TRUE)
  expect_match(out$code, "mydata.csv", fixed = TRUE)
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `Rscript -e 'devtools::test(filter = "logistic")'`
Expected: FAIL — `out$code` is `""` (parse/`glm(` match fails).

- [ ] **Step 3: Write minimal implementation**

Add to `R/logistic.R` (mirrors `.cox_script`; deparses the actual glm calls):

```r
# Downloadable R script: the exact univariable + joint glm calls, plus an
# equivalent forest plot. For uploads, prep reads the user's REAL column names +
# event coding (source_roles), exactly like .cox_script.
.logistic_script <- function(spec, p, fits) {
  opts <- spec$options %||% list()
  qe <- function(s) gsub('"', '\\\\"', s)
  covs <- p$covs
  sr <- if (nzchar(as.character(opts$source_filename %||% ""))) opts$source_roles else NULL
  ev <- qe(as.character(opts$event_value %||% ""))
  outcome_name <- if (!is.null(sr)) sr$outcome else spec$roles$outcome
  ocol_raw <- sprintf('df[["%s"]]', qe(outcome_name))
  cov_expr <- function(cl) sprintf('df[["%s"]]', qe(cl))

  prep <- c(
    sprintf('# Event coding: outcome == "%s" is the event (y = 1); all else y = 0.', ev),
    sprintf('outcome_raw <- %s', ocol_raw),
    sprintf(paste0('y <- as.integer(as.character(outcome_raw) == "%s" | ',
                   '(!is.na(suppressWarnings(as.numeric("%s"))) & ',
                   '!is.na(suppressWarnings(as.numeric(outcome_raw))) & ',
                   'suppressWarnings(as.numeric(outcome_raw)) == suppressWarnings(as.numeric("%s"))))'),
            ev, ev, ev),
    sprintf('dat <- data.frame(.y = y,'),
    paste0("                  ",
      paste(vapply(covs, function(cl) sprintf('`%s` = %s', qe(cl), cov_expr(cl)),
                   character(1)), collapse = ",\n                  "), ","),
    "                  check.names = FALSE, stringsAsFactors = FALSE)",
    "dat <- dat[complete.cases(dat), ]")

  incr_lines <- unlist(lapply(covs, function(cl) {
    if (p$cov_types[[cl]] != "numeric" || p$incr[[cl]] == 1) return(NULL)
    sprintf('dat[["%s"]] <- dat[["%s"]] / %g   # per-%g-unit odds ratio',
            qe(cl), qe(cl), p$incr[[cl]], p$incr[[cl]])
  }))

  relevel_lines <- unlist(lapply(covs, function(cl) {
    if (p$cov_types[[cl]] != "categorical") return(NULL)
    ref <- qe(as.character(p$ref_levels[[cl]] %||% .logistic_most_frequent(p$df[[cl]])))
    sprintf('dat[["%s"]] <- relevel(factor(dat[["%s"]]), ref = "%s")', qe(cl), qe(cl), ref)
  }))

  uni_lines <- unlist(lapply(covs, function(cl) c(
    sprintf('# Unadjusted OR for %s', cl),
    sprintf('summary(glm(.y ~ `%s`, family = binomial, data = dat))', qe(cl)),
    sprintf('exp(cbind(OR = coef(.m <- glm(.y ~ `%s`, family = binomial, data = dat)), confint.default(.m)))', qe(cl)),
    "")))
  joint_rhs <- paste(sprintf("`%s`", covs), collapse = " + ")
  joint_lines <- c(
    "# Adjusted (joint) model:",
    sprintf("fit <- glm(.y ~ %s, family = binomial, data = dat)", joint_rhs),
    "summary(fit)",
    "exp(cbind(OR = coef(fit), confint.default(fit)))   # adjusted ORs + Wald 95% CI", "",
    "# Overall discrimination (C-statistic):",
    "prob <- fitted(fit); n1 <- sum(dat$.y == 1); n0 <- sum(dat$.y == 0)",
    "(sum(rank(prob)[dat$.y == 1]) - n1 * (n1 + 1) / 2) / (n1 * n0)", "")
  fig_lines <- c(
    "# Equivalent forest plot of the adjusted odds ratios:",
    "library(ggplot2)",
    "co <- exp(cbind(coef(fit), confint.default(fit)))[-1, , drop = FALSE]",
    "fp <- data.frame(term = rownames(co), or = co[, 1], lo = co[, 2], hi = co[, 3])",
    "fp$term <- factor(fp$term, levels = rev(fp$term))",
    "p_forest <- ggplot(fp, aes(or, term)) +",
    '  geom_vline(xintercept = 1, linetype = "dashed", colour = "grey50") +',
    '  geom_errorbar(aes(xmin = lo, xmax = hi), orientation = "y", width = 0.2) +',
    "  geom_point(size = 2.4) + scale_x_log10() +",
    '  labs(x = "Adjusted odds ratio (log scale)", y = NULL) +',
    "  theme_minimal(base_size = 12)",
    '# print(p_forest)')

  body <- c(prep, "",
            incr_lines, if (length(incr_lines)) "" else NULL,
            relevel_lines, if (length(relevel_lines)) "" else NULL,
            uni_lines, joint_lines, fig_lines)
  .script_assemble("Logistic regression", spec,
                   c(spec$roles$outcome, covs), c("ggplot2"), body)
}
```

In `fig_logistic`, change the return `code = ""` to `code = .logistic_script(spec, p, fits)`.

- [ ] **Step 4: Run test to verify it passes**

Run: `Rscript -e 'devtools::test(filter = "logistic")'`
Expected: PASS, `[ FAIL 0 | WARN 0 | ... ]`.

- [ ] **Step 5: Commit**

```bash
rm -rf web/R && cp -R R web/R
git add R/logistic.R tests/testthat/test-logistic.R
git commit -m "feat(logistic): downloadable R script (deparsed glm calls, embed/read.csv)"
```

---

## Task 5: Dispatcher + worker wiring

**Files:**
- Modify: `R/dispatch.R:8-14`
- Modify: `web/worker.js:42`
- Modify: `tests/testthat/test-logistic.R`

**Interfaces:**
- Consumes: `render_figure(json_string)` (`R/dispatch.R`); `fig_logistic` (Task 1-4).
- Produces: `spec$figure=="logistic"` routes to `fig_logistic`; the worker fetches `R/logistic.R` at boot.

- [ ] **Step 1: Write the failing test**

Append to `tests/testthat/test-logistic.R`:

```r
test_that("render_figure routes logistic specs and returns ok JSON", {
  spec <- sc_logit(mk_logit_rows())
  js <- jsonlite::toJSON(spec, auto_unbox = TRUE)
  res <- jsonlite::fromJSON(render_figure(js))
  expect_true(res$ok)
  expect_match(res$svg, "<table", fixed = TRUE)
  expect_true(nzchar(res$code))
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `Rscript -e 'devtools::test(filter = "logistic")'`
Expected: FAIL — `res$ok` is FALSE (`Unknown figure: logistic`).

- [ ] **Step 3: Write minimal implementation**

In `R/dispatch.R`, add to the `switch` (after the `cox` line):

```r
      cox     = fig_cox(spec),
      logistic = fig_logistic(spec),
      stop(sprintf("Unknown figure: %s", fig))
```

In `web/worker.js:42`, add `"logistic.R"` to the fetch loop:

```js
  for (const f of ["dispatch.R", "script.R", "summarize.R", "km.R", "themes.R", "explore.R", "groupcompare.R", "cox.R", "logistic.R"]) {
```

Do **not** add a `logistic:` key to `EXTRA_PACKAGES` — the module is base `stats` + boot packages only.

- [ ] **Step 4: Run test to verify it passes**

Run: `Rscript -e 'devtools::test(filter = "logistic")'`
Expected: PASS, `[ FAIL 0 | WARN 0 | ... ]`.

- [ ] **Step 5: Commit**

```bash
rm -rf web/R && cp -R R web/R
git add R/dispatch.R web/worker.js tests/testthat/test-logistic.R
git commit -m "feat(logistic): route logistic through dispatch + boot-fetch R/logistic.R"
```

---

## Task 6: Frozen synthetic demo dataset

**Files:**
- Create: `data-raw/logistic-demo-generator.R`
- Create: `web/guided/logistic/demo-data.js` (generated output — committed)
- Create: `tests/testthat/fixtures/logistic-demo.csv` (generated output — committed)

**Interfaces:**
- Produces: `web/guided/logistic/demo-data.js` exporting `LOGISTIC_DEMO = { version, label, columns, rows }` with columns `["arm", "age", "stage", "complication"]`; outcome `complication` in {`Yes`,`No`}, engineered so the unadjusted `arm` OR is near 1 but the adjusted OR is protective (~0.5). Consumed by Tasks 7-9.

- [ ] **Step 1: Write the generator**

Create `data-raw/logistic-demo-generator.R` (mirrors `data-raw/cox-demo-generator.R`):

```r
# data-raw/logistic-demo-generator.R
# Frozen logistic-regression demo. Binary outcome `complication` (Yes/No), two
# arms (Standard care / New treatment), baseline age and ordinal stage (I/II/III).
# Engineered so age + stage confound the arm effect: the UNADJUSTED arm OR is near
# 1, but adjusting reveals a protective adjusted OR (~0.5). Byte-reproducible;
# bump LOGISTIC_DEMO.version on any change.
set.seed(74)
n <- 240
arm <- rep(c("Standard care", "New treatment"), each = n / 2)
age <- round(rnorm(n, 60, 9))
stage <- sample(c("I", "II", "III"), n, replace = TRUE, prob = c(0.4, 0.35, 0.25))
# Channeling by indication: the New treatment arm skews older/sicker.
sicker <- (arm == "New treatment")
age <- round(age + 6 * sicker)
stage_idx <- as.integer(factor(stage, levels = c("I", "II", "III")))
lp <- -1.1 + 0.06 * (age - 60) + 0.55 * (stage_idx - 1) - 0.75 * (arm == "New treatment")
complication <- ifelse(runif(n) < plogis(lp), "Yes", "No")

out <- data.frame(arm = arm, age = age, stage = stage,
                  complication = complication, stringsAsFactors = FALSE)
write.csv(out, "tests/testthat/fixtures/logistic-demo.csv",
          row.names = FALSE, quote = FALSE)

rows_list <- lapply(seq_len(nrow(out)), function(i) as.list(out[i, ]))
rows_json <- jsonlite::toJSON(rows_list, auto_unbox = TRUE, digits = NA)
js <- paste0(
  "// GENERATED by data-raw/logistic-demo-generator.R — do not hand-edit.\n",
  "export const LOGISTIC_DEMO = {\n",
  '  version: "1.0.0",\n',
  '  label: "Synthetic demonstration data",\n',
  '  columns: ["arm", "age", "stage", "complication"],\n',
  "  rows: ", rows_json, "\n};\n")
writeLines(js, "web/guided/logistic/demo-data.js")
cat("md5(csv):", unname(tools::md5sum("tests/testthat/fixtures/logistic-demo.csv")), "\n")
```

- [ ] **Step 2: Run the generator and verify the demo story**

Run:
```bash
mkdir -p web/guided/logistic
Rscript data-raw/logistic-demo-generator.R
Rscript -e 'd <- read.csv("tests/testthat/fixtures/logistic-demo.csv"); d$y <- as.integer(d$complication=="Yes"); cat("unadj:", exp(coef(glm(y~arm,binomial,d))["armNew treatment"]), " adj:", exp(coef(glm(y~arm+age+stage,binomial,d))["armNew treatment"]), "\n")'
```
Expected: `demo-data.js` and `logistic-demo.csv` created; printed unadjusted OR near 1 (roughly 0.8–1.3) and adjusted OR clearly below 1 (roughly 0.4–0.6). If the story is weak, adjust the `lp` coefficients and regenerate (do not hand-edit outputs).

- [ ] **Step 3: Commit**

```bash
git add data-raw/logistic-demo-generator.R web/guided/logistic/demo-data.js tests/testthat/fixtures/logistic-demo.csv
git commit -m "feat(logistic): frozen synthetic demo dataset (confounded arm effect)"
```

---

## Task 7: Web spec assembly + JS unit test

**Files:**
- Create: `web/guided/logistic/spec.js`
- Create: `web/guided/logistic/spec.test.mjs`

**Interfaces:**
- Consumes: `web/guided/logistic/demo-data.js` (Task 6).
- Produces: `buildLogisticSpec(table, roles, eventValue, refLevels, increments, options)`, `distinctValues(table, col)`, `mostFrequent(table, col)`. Spec shape: `{figure:"logistic", data:[...], roles:{outcome, covariates}, options:{event_value, ref_levels, increments, source_filename, source_roles}}`. `roles.covariates` is an array; `data` rows carry only mapped columns (no-egress).

- [ ] **Step 1: Write the failing test**

Create `web/guided/logistic/spec.test.mjs` (plain Node asserts, mirrors `web/guided/cox/spec.test.mjs`):

```js
import assert from "node:assert";
import { buildLogisticSpec, distinctValues, mostFrequent } from "./spec.js";

const table = {
  columns: ["outcome", "arm", "age", "note"],
  rows: [
    { outcome: "Yes", arm: "A", age: "60", note: "x" },
    { outcome: "No", arm: "B", age: "70", note: "y" },
    { outcome: "Yes", arm: "A", age: "65", note: "z" },
  ],
};
const roles = { outcome: "outcome", covariates: ["arm", "age"] };

const spec = buildLogisticSpec(table, roles, "Yes", { arm: "B" }, { age: 10 },
  { source_filename: "f.csv" });
assert.equal(spec.figure, "logistic");
assert.deepEqual(spec.roles, { outcome: "outcome", covariates: ["arm", "age"] });
assert.equal(spec.options.event_value, "Yes");
assert.deepEqual(spec.options.ref_levels, { arm: "B" });
assert.deepEqual(spec.options.increments, { age: 10 });
// no-egress: only mapped columns cross, `note` is dropped
assert.deepEqual(Object.keys(spec.data[0]).sort(), ["age", "arm", "outcome"]);
assert.equal(spec.options.source_roles.outcome, "outcome");
assert.equal(spec.options.source_roles.event, "Yes");

assert.deepEqual(distinctValues(table, "arm"), ["A", "B"]);
assert.equal(mostFrequent(table, "arm"), "A");
console.log("ok - logistic spec");
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node web/guided/logistic/spec.test.mjs`
Expected: FAIL — `Cannot find module './spec.js'`.

- [ ] **Step 3: Write minimal implementation**

Create `web/guided/logistic/spec.js` (mirrors `web/guided/cox/spec.js`):

```js
// Pure spec assembly for logistic regression. Only mapped columns cross to the
// worker (no-egress); the outcome stays a raw string (R recodes it from
// event_value). source_roles lets the .R script read the user's real headers.
export function buildLogisticSpec(table, roles, eventValue, refLevels, increments, options) {
  const used = [roles.outcome, ...roles.covariates];
  const data = table.rows.map((r) =>
    Object.fromEntries(used.map((c) => [c, r[c]])));
  return {
    figure: "logistic",
    data,
    roles: { outcome: roles.outcome, covariates: roles.covariates.slice() },
    options: {
      event_value: String(eventValue),
      ref_levels: refLevels || {},
      increments: increments || {},
      source_filename: options.source_filename ?? null,
      source_roles: { outcome: roles.outcome, covariates: roles.covariates.slice(),
                      event: String(eventValue) },
    },
  };
}

// Distinct non-blank values, first-appearance order (feeds the event picker and
// each categorical covariate's reference-level dropdown).
export function distinctValues(table, col) {
  const seen = [];
  for (const r of table.rows) {
    const v = r[col];
    if (v == null || String(v).trim() === "") continue;
    const s = String(v);
    if (!seen.includes(s)) seen.push(s);
  }
  return seen;
}

// Most frequent non-blank value of a column (default reference level).
export function mostFrequent(table, col) {
  const counts = new Map();
  for (const r of table.rows) {
    const v = r[col];
    if (v == null || String(v).trim() === "") continue;
    const s = String(v);
    counts.set(s, (counts.get(s) || 0) + 1);
  }
  let best = null, n = -1;
  for (const [k, c] of counts) if (c > n) { best = k; n = c; }
  return best;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node web/guided/logistic/spec.test.mjs` then `npm run test:unit`
Expected: `ok - logistic spec`; the unit suite passes (it globs `web/**/*.test.mjs`).

- [ ] **Step 5: Commit**

```bash
git add web/guided/logistic/spec.js web/guided/logistic/spec.test.mjs
git commit -m "feat(logistic): pure spec builder + JS unit test (no-egress, increments)"
```

---

## Task 8: Analyze-your-data upload form

**Files:**
- Create: `web/guided/logistic/analyze-form.js`

**Interfaces:**
- Consumes: `parseCsv`, `toCsv` (`web/lib/csv.js`); `renderColumnPicker` (`web/lib/columnpicker.js`); `buildLogisticSpec`, `distinctValues`, `mostFrequent` (Task 7); `LOGISTIC_DEMO` (Task 6).
- Produces: `renderLogisticAnalyzeForm(container, onSubmit, doc = globalThis.document)`. Column picker roles: `outcome` (any, single), `covariates` (any, multiple). An event-value `<select>` over the **outcome** column; a reference-level `<select>` per categorical covariate; an increment `<input type="number">` per numeric covariate. Calls `onSubmit(spec)` with `buildLogisticSpec(...)`.

- [ ] **Step 1: Write the implementation**

This UI is DOM-wiring exercised by the Task 11 e2e test (no separate unit test — it needs a DOM). Create `web/guided/logistic/analyze-form.js` (adapts `web/guided/cox/analyze-form.js`: outcome replaces time+status; event picker targets the outcome; adds per-numeric increment inputs):

```js
// web/guided/logistic/analyze-form.js
// Progressive-disclosure upload UI for logistic regression, on the shared
// csv/columnpicker foundation. The config (roles, event value, reference levels,
// per-numeric increments, Render) stays hidden until a CSV parse succeeds; a
// parse failure re-hides it and clears state. Mirrors web/guided/cox/analyze-form.js.
import { parseCsv, toCsv } from "../../lib/csv.js";
import { renderColumnPicker } from "../../lib/columnpicker.js";
import { buildLogisticSpec, distinctValues, mostFrequent } from "./spec.js";
import { LOGISTIC_DEMO } from "./demo-data.js";

let exampleCsvUrl = null;
function getExampleCsvUrl() {
  if (!exampleCsvUrl) {
    const blob = new Blob([toCsv(LOGISTIC_DEMO.rows, LOGISTIC_DEMO.columns)],
      { type: "text/csv" });
    exampleCsvUrl = URL.createObjectURL(blob);
  }
  return exampleCsvUrl;
}

export function renderLogisticAnalyzeForm(container, onSubmit, doc = globalThis.document) {
  container.innerHTML = `
    <h2>Analyze your data</h2>
    <p>Your file is read locally in this browser and never uploaded.</p>
    <details class="csv-help">
      <summary>What your CSV should look like</summary>
      <ul>
        <li>One row per participant, one column per variable.</li>
        <li>A binary outcome column — any coding
          (<code>Yes</code>/<code>No</code>, <code>1</code>/<code>0</code>, …);
          you confirm which value is the event below.</li>
        <li>One or more covariate columns to adjust for (numeric or categorical).</li>
        <li>Leave a cell empty when a value is missing.</li>
      </ul>
      <p><a id="example-csv" download="example-logistic.csv" href="#">Download an example CSV</a>
        — the synthetic teaching dataset from the Example tab.</p>
    </details>
    <label for="csv">CSV file</label>
    <input type="file" id="csv" accept=".csv" />
    <div id="logistic-config" hidden></div>`;
  container.querySelector("#example-csv").href = getExampleCsvUrl();
  const config = container.querySelector("#logistic-config");
  let table = null, roles = null, fileName = null;

  function showError(message) {
    const stats = doc.getElementById("stats");
    stats.textContent = "Error: " + message;
    stats.classList.add("error");
  }

  container.querySelector("#csv").onchange = (e) => {
    const file = e.target.files && e.target.files[0];
    if (!file) return;
    const reader = new FileReader();
    reader.onload = () => {
      try {
        table = parseCsv(reader.result);
        fileName = file.name;
        doc.getElementById("stats").classList.remove("error");
        config.innerHTML = "";
        const pick = doc.createElement("div"); config.appendChild(pick);

        // Built BEFORE renderColumnPicker (its onReady fires synchronously,
        // including once with null), so everything it touches already exists.
        const eventLabel = doc.createElement("label");
        eventLabel.textContent =
          "Which value of the outcome column means the event occurred? ";
        const eventSel = doc.createElement("select"); eventSel.id = "logistic-event";
        eventLabel.htmlFor = "logistic-event";
        const eventHint = doc.createElement("p");
        eventHint.className = "hint";
        eventHint.textContent = "All other values count as non-events.";

        const refWrap = doc.createElement("div"); refWrap.id = "logistic-refs";
        const incrWrap = doc.createElement("div"); incrWrap.id = "logistic-increments";

        const btn = doc.createElement("button");
        btn.type = "button"; btn.id = "logistic-render";
        btn.textContent = "Render logistic model"; btn.disabled = true;
        const note = doc.createElement("p");
        note.id = "logistic-dropped-note"; note.className = "hint";

        let outcomeCol = null;
        const isNumericCol = (col) => table.types[col] === "numeric";
        const syncReady = () => {
          btn.disabled = !roles || !roles.covariates
            || roles.covariates.length === 0 || !eventSel.value;
        };
        const fillEventOptions = () => {
          eventSel.innerHTML = "";
          const ph = doc.createElement("option");
          ph.value = ""; ph.textContent = "— choose —";
          eventSel.appendChild(ph);
          if (outcomeCol) for (const v of distinctValues(table, outcomeCol)) {
            const o = doc.createElement("option");
            o.value = v; o.textContent = v;
            eventSel.appendChild(o);
          }
          eventSel.disabled = !outcomeCol;
          syncReady();
        };
        const renderRefs = () => {
          refWrap.innerHTML = "";
          if (!roles || !roles.covariates) return;
          for (const c of roles.covariates) {
            if (isNumericCol(c)) continue;   // numeric -> increment, not a reference
            const l = doc.createElement("label");
            l.textContent = `Reference level for ${c} `;
            const s = doc.createElement("select");
            s.id = "logistic-ref-" + c; s.dataset.cov = c;
            for (const v of distinctValues(table, c)) {
              const o = doc.createElement("option");
              o.value = v; o.textContent = v;
              s.appendChild(o);
            }
            s.value = mostFrequent(table, c);
            l.appendChild(s); refWrap.appendChild(l);
          }
        };
        const renderIncrements = () => {
          incrWrap.innerHTML = "";
          if (!roles || !roles.covariates) return;
          for (const c of roles.covariates) {
            if (!isNumericCol(c)) continue;   // categorical -> reference, not an increment
            const l = doc.createElement("label");
            l.textContent = `Report ${c} per (increment) `;
            const inp = doc.createElement("input");
            inp.type = "number"; inp.min = "0"; inp.step = "any"; inp.value = "1";
            inp.id = "logistic-incr-" + c; inp.dataset.cov = c;
            l.appendChild(inp); incrWrap.appendChild(l);
          }
        };

        renderColumnPicker(pick,
          [{ key: "outcome", label: "Binary outcome", type: "any" },
           { key: "covariates", label: "Covariates to adjust for", type: "any",
             multiple: true }],
          table, (v) => {
            roles = v;
            const o = v ? v.outcome : null;
            if (o !== outcomeCol) { outcomeCol = o; fillEventOptions(); }
            renderRefs(); renderIncrements();
            syncReady();
          }, doc);
        eventSel.onchange = syncReady;
        fillEventOptions();

        config.appendChild(eventLabel);
        eventLabel.appendChild(eventSel);
        config.appendChild(eventHint);
        config.appendChild(refWrap);
        config.appendChild(incrWrap);

        btn.onclick = () => {
          if (!roles || !eventSel.value) return;
          const refLevels = {};
          refWrap.querySelectorAll("select[data-cov]").forEach((s) => {
            refLevels[s.dataset.cov] = s.value;
          });
          const increments = {};
          incrWrap.querySelectorAll("input[data-cov]").forEach((inp) => {
            const k = Number(inp.value);
            if (Number.isFinite(k) && k > 0) increments[inp.dataset.cov] = k;
          });
          const spec = buildLogisticSpec(table, roles, eventSel.value, refLevels,
            increments, { source_filename: fileName });
          const used = [roles.outcome, ...roles.covariates];
          const dropped = table.rows.filter((r) =>
            used.some((c) => r[c] == null || String(r[c]).trim() === "")).length;
          note.textContent = dropped > 0
            ? `${dropped} row(s) with missing values will be excluded.` : "";
          onSubmit(spec);
        };
        config.appendChild(btn);
        config.appendChild(note);
        config.hidden = false;
      } catch (err) {
        table = null; config.hidden = true; config.innerHTML = "";
        showError(err.message);
      }
    };
    reader.readAsText(file);
  };
}
```

- [ ] **Step 2: Verify it parses (no DOM here; full exercise is Task 11 e2e)**

Run: `node --check web/guided/logistic/analyze-form.js`
Expected: no output (syntax OK).

- [ ] **Step 3: Commit**

```bash
git add web/guided/logistic/analyze-form.js
git commit -m "feat(logistic): analyze upload form — outcome event picker, refs, per-numeric increments"
```

---

## Task 9: Understand/Example content + demo wiring + shell config

**Files:**
- Create: `web/guided/logistic/content.js`
- Create: `web/guided/logistic/demo.js`
- Create: `web/guided/logistic/guided-logistic.js`

**Interfaces:**
- Consumes: `createGuidedShell` (`web/guided/shell.js`); `LOGISTIC_DEMO` (Task 6); the content/demo/form pieces here + Task 8.
- Produces: `renderGuidedLogistic` (default-exported shell renderer), imported by `web/app.js` in Task 10. `buildLogisticDemoSpec(demoState)` sends `{figure:"logistic", roles:{outcome:"complication", covariates}, options:{event_value:"Yes", ref_levels:{arm,stage}, increments:{age:10}, caption}}`.

- [ ] **Step 1: Write the content module**

Create `web/guided/logistic/content.js` (adapts `web/guided/cox/content.js`):

```js
// web/guided/logistic/content.js
import { LOGISTIC_DEMO } from "./demo-data.js";

const SECTIONS = [
  { title: "Adjust for what else is going on", html: `
    <p>A group comparison tells you whether an outcome is more common in one group.
    Logistic regression answers the next question: <em>by how much, after accounting
    for the other things that differ between patients?</em> Each covariate gets an
    odds ratio — the relative odds of the outcome per unit (for a number) or versus a
    reference level (for a category).</p>
    <p>Unadjusted odds ratios come from one model per covariate. Adjusted odds ratios
    come from a single joint model, so each is the effect of that variable holding the
    others fixed — the difference between the two columns is the confounding the
    adjustment removed.</p>` },
  { title: "Is logistic regression appropriate?", html: `
    <p>Use it when each row is one independent participant, the outcome is
    <strong>binary</strong> (event / no event), and you have the baseline covariates
    you want to adjust for. The tool needs at least about 10 events in the smaller
    outcome group to fit a stable model.</p>
    <p>It checks for <strong>separation</strong> (a covariate that predicts the outcome
    perfectly), <strong>multicollinearity</strong> (redundant covariates), and reports
    the model's overall discrimination — each non-blocking. It does not fit penalized
    (Firth) or mixed models; if separation is flagged, seek statistical review.</p>` },
  { title: "How to read the result", html: `
    <ul>
      <li><strong>OR &lt; 1</strong>: lower odds of the outcome (protective). <strong>OR &gt; 1</strong>: higher.</li>
      <li>A numeric covariate's OR is <strong>per increment</strong> — set the increment (e.g. per 10 years) so one step is clinically meaningful.</li>
      <li>A category's OR is <strong>versus its reference level</strong>, shown as "1 (reference)".</li>
      <li>A 95% CI that crosses 1 means the effect is not statistically resolved.</li>
    </ul>
    <p class="callout">With fewer than about 10 events per model term, adjusted
    estimates become unstable — the tool warns you when that happens.</p>` },
];

export function renderUnderstand(panel) {
  panel.innerHTML = SECTIONS.map((s) => `<section><h3>${s.title}</h3>${s.html}</section>`).join("");
}

export const EXAMPLE_INTRO_HTML = `
  <h3>Explore a synthetic outcome study</h3>
  <p>This teaching dataset has 240 fictional patients on <strong>Standard care</strong>
  or a <strong>New treatment</strong>, with baseline age and disease stage, and whether a
  <strong>complication</strong> occurred. The treatment was given preferentially to older,
  higher-stage patients — so its unadjusted odds ratio looks null, and only adjusting for
  age and stage reveals it. Toggle the covariates below to watch the treatment's adjusted
  odds ratio move.</p>`;

// Experiments: check/uncheck which covariates enter the joint model. `arm` is the
// exposure of interest and is always included (its checkbox is disabled).
export function renderLogisticExperiments(panel, ctx, rerun) {
  const host = panel.querySelector("#demo-experiments");
  host.innerHTML = "";
  const ALL = ["arm", "age", "stage"];
  const state = ctx.getSession().demoOptions;
  const fieldset = document.createElement("fieldset");
  const legend = document.createElement("legend");
  legend.textContent = "Covariates in the model";
  fieldset.appendChild(legend);
  for (const c of ALL) {
    const label = document.createElement("label");
    label.className = "inline-check";
    const cb = document.createElement("input");
    cb.type = "checkbox"; cb.id = "cov-" + c; cb.value = c;
    cb.checked = state.covariates.includes(c);
    cb.disabled = c === "arm";
    cb.onchange = () => {
      const now = ctx.getSession().demoOptions.covariates.filter((x) => x !== c);
      if (cb.checked) now.push(c);
      ctx.patchDemoOptions({ covariates: ALL.filter((x) => now.includes(x)) });
      rerun();
    };
    label.appendChild(cb);
    label.appendChild(document.createTextNode(" " + c));
    fieldset.appendChild(label);
  }
  host.appendChild(fieldset);
}
```

- [ ] **Step 2: Write the demo module**

Create `web/guided/logistic/demo.js` (adapts `web/guided/cox/demo.js`; `age` carries a per-10-year increment to showcase the feature):

```js
import { LOGISTIC_DEMO } from "./demo-data.js";

export const DEMO_TABLE = {
  columns: LOGISTIC_DEMO.columns,
  rows: LOGISTIC_DEMO.rows,
  types: { arm: "categorical", age: "numeric", stage: "categorical",
           complication: "categorical" },
};

// Fresh object each call — the guided session store resets by shallow copy.
export function DEFAULT_DEMO_STATE() {
  return { covariates: ["arm", "age", "stage"] };
}

// The demo goes through the SAME spec shape + worker + R path as user data. Only
// the mapped columns cross (no-egress). Ref levels + the age increment are fixed
// so the confounding story is stable and age reads per 10 years.
export function buildLogisticDemoSpec(demoState) {
  const used = ["complication", ...demoState.covariates];
  const data = LOGISTIC_DEMO.rows.map((r) =>
    Object.fromEntries(used.map((c) => [c, r[c]])));
  return {
    figure: "logistic",
    data,
    roles: { outcome: "complication", covariates: demoState.covariates.slice() },
    options: { event_value: "Yes",
               ref_levels: { arm: "Standard care", stage: "I" },
               increments: { age: 10 },
               caption: LOGISTIC_DEMO.label },
  };
}
```

- [ ] **Step 3: Write the shell config**

Create `web/guided/logistic/guided-logistic.js` (mirrors `web/guided/cox/guided-cox.js`):

```js
// web/guided/logistic/guided-logistic.js
import { createGuidedShell } from "../shell.js";
import { renderUnderstand, EXAMPLE_INTRO_HTML, renderLogisticExperiments } from "./content.js";
import { buildLogisticDemoSpec, DEFAULT_DEMO_STATE } from "./demo.js";
import { LOGISTIC_DEMO } from "./demo-data.js";
import { renderLogisticAnalyzeForm } from "./analyze-form.js";

export const renderGuidedLogistic = createGuidedShell({
  title: "Logistic regression",
  hashPrefix: "logistic",
  renderUnderstand,
  exampleIntroHtml: EXAMPLE_INTRO_HTML,
  demoLabel: LOGISTIC_DEMO.label,
  buildDemoSpec: buildLogisticDemoSpec,
  defaultDemoOptions: DEFAULT_DEMO_STATE,
  experimentControlsSelector: "#demo-experiments input",
  renderExperiments: renderLogisticExperiments,
  renderAnalyzeForm: renderLogisticAnalyzeForm,
});
```

- [ ] **Step 4: Verify the modules parse**

Run: `node --check web/guided/logistic/content.js && node --check web/guided/logistic/demo.js && node --check web/guided/logistic/guided-logistic.js`
Expected: no output (all syntax OK).

- [ ] **Step 5: Commit**

```bash
git add web/guided/logistic/content.js web/guided/logistic/demo.js web/guided/logistic/guided-logistic.js
git commit -m "feat(logistic): guided content, demo wiring (age per 10y), shell config"
```

---

## Task 10: Register the analysis (nav, registry, service worker, smoke test)

**Files:**
- Modify: `web/app.js:107-111`
- Modify: `web/index.html` (after the Cox button, ~line 45)
- Modify: `web/sw.js` (cache-name bump)
- Modify: `tests/e2e/smoke.spec.js`

**Interfaces:**
- Consumes: `renderGuidedLogistic` (Task 9).
- Produces: a `Logistic regression` nav button (sixth) routing to the guided shell; smoke test expects six analyses.

- [ ] **Step 1: Write the failing test**

Edit `tests/e2e/smoke.spec.js` to expect six analyses (Logistic after Cox, before Explore — matching nav DOM order set in Step 3):

```js
test("app boots with exactly the six guided analyses", async ({ page }) => {
  await page.goto("/");
  const nav = page.locator("nav button[data-figure]");
  await expect(nav).toHaveCount(6);
  const labels = nav.locator(".nav-label");
  await expect(labels.nth(0)).toHaveText("Summary statistics");
  await expect(labels.nth(1)).toHaveText("Kaplan-Meier");
  await expect(labels.nth(2)).toHaveText("Group comparison");
  await expect(labels.nth(3)).toHaveText("Cox regression");
  await expect(labels.nth(4)).toHaveText("Logistic regression");
  await expect(labels.nth(5)).toHaveText("Explore plot");
  await expect(page.getByText("Select an analysis on the left to begin.")).toBeVisible();
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `rm -rf web/R && cp -R R web/R && npm run test:e2e -- smoke`
Expected: FAIL — nav has 5, not 6 (and no Logistic label).

- [ ] **Step 3: Write minimal implementation**

In `web/app.js`, add the import (near line 107) and registry entry (near line 111):

```js
import { renderGuidedCox } from "./guided/cox/guided-cox.js";
import { renderGuidedLogistic } from "./guided/logistic/guided-logistic.js";
```

```js
                cox: renderGuidedCox,
                logistic: renderGuidedLogistic };
```

In `web/index.html`, add the button after the Cox `<button data-figure="cox">…</button>` block and before the Explore button:

```html
        <button data-figure="logistic">
          <span class="nav-label">Logistic regression</span>
        </button>
```

In `web/sw.js`, bump the cache constant (find the current `figura-v*` name and increment), e.g.:

```js
const CACHE = "figura-v4";
```

- [ ] **Step 4: Run test to verify it passes**

Run: `rm -rf web/R && cp -R R web/R && npm run test:e2e -- smoke`
Expected: PASS — six analyses in order.

- [ ] **Step 5: Commit**

```bash
git add web/app.js web/index.html web/sw.js tests/e2e/smoke.spec.js
git commit -m "feat(logistic): register nav + form registry; bump sw cache; smoke expects six"
```

---

## Task 11: End-to-end guided test (demo + upload)

**Files:**
- Create: `tests/e2e/logistic-guided.spec.js`

**Interfaces:**
- Consumes: the full running app (Tasks 1-10). Exercises the demo render, the `.R` download enablement, and the upload path with per-numeric increments.

- [ ] **Step 1: Write the test**

Create `tests/e2e/logistic-guided.spec.js` (adapts `tests/e2e/cox-guided.spec.js`):

```js
// tests/e2e/logistic-guided.spec.js
const { test, expect } = require("@playwright/test");

test("logistic shows three tabs and syncs the hash", async ({ page }) => {
  await page.goto("/");
  await page.getByRole("button", { name: /logistic regression/i }).click();
  await expect(page.getByRole("tab", { name: "Understand" })).toBeVisible();
  await page.getByRole("tab", { name: "Try an Example" }).click();
  expect(page.url()).toContain("#logistic/example");
});

test("demo fits a Table-3 + forest plot and enables the .R download", async ({ page }) => {
  test.setTimeout(360000);
  await page.goto("/#logistic/example");
  await page.getByRole("button", { name: /logistic regression/i }).click();
  await page.getByRole("button", { name: "Run Example Analysis" }).click();
  await expect(page.locator("#preview table")).toBeVisible({ timeout: 300000 });
  await expect(page.locator("#preview svg")).toBeVisible();
  await expect(page.locator("#stats")).toContainText(/adjusted/i);
  await expect(page.locator("#stats")).toContainText(/New treatment/);
  await expect(page.locator("#stats")).toContainText(/C-statistic/);
  await expect(page.locator("#export-r")).toBeEnabled();
});

test("analyze stage fits an uploaded logistic model with adjusted ORs", async ({ page }) => {
  test.setTimeout(360000);
  await page.goto("/#logistic/analyze");
  await page.getByRole("button", { name: /logistic regression/i }).click();
  await expect(page.locator("#csv")).toBeVisible();
  await expect(page.locator("#logistic-config")).toBeHidden();
  // Synthetic CSV: binary outcome (Event/NoEvent), an arm, and a numeric age.
  const rows = Array.from({ length: 120 }, (_, i) => {
    const arm = i % 2 ? "Treated" : "Control";
    const age = 50 + (i % 25);
    const event = (i % 3 === 0) ? "Event" : "NoEvent";
    return `${event},${arm},${age}`;
  });
  const csv = "outcome,arm,age\n" + rows.join("\n");
  await page.locator("#csv").setInputFiles({
    name: "logistic.csv", mimeType: "text/csv", buffer: Buffer.from(csv) });
  // Map roles: outcome -> outcome, covariates -> arm + age.
  await expect(page.locator("#logistic-config")).toBeVisible();
  await page.locator("#logistic-event").selectOption("Event");
  // Set the age increment to 10 (per 10 years).
  await page.locator("#logistic-incr-age").fill("10");
  await page.getByRole("button", { name: "Render logistic model" }).click();
  await expect(page.locator("#preview table")).toBeVisible({ timeout: 300000 });
  await expect(page.locator("#stats")).toContainText(/adjusted/i);
});
```

Note: the role-mapping dropdowns are driven by `renderColumnPicker`; if the Cox e2e selects roles via specific selectors, match that pattern here (inspect `tests/e2e/cox-guided.spec.js` lines beyond 40 for the exact `selectOption`/picker interaction and mirror it — the column-picker markup is shared, so the same locators apply with role keys `outcome`/`covariates`).

- [ ] **Step 2: Run the test**

Run: `rm -rf web/R && cp -R R web/R && npm run test:e2e -- logistic-guided`
Expected: PASS (webR download makes the first run slow; the 300s timeouts cover it).

- [ ] **Step 3: Commit**

```bash
git add tests/e2e/logistic-guided.spec.js
git commit -m "test(logistic): e2e demo + analyze flow with per-10-year age increment"
```

---

## Task 12: Documentation + memory

**Files:**
- Modify: `CLAUDE.md`
- Create: `/Users/sanman/.claude/projects/-Users-sanman-Documents-my-stats/memory/session-2026-07-17-logistic-regression.md`
- Modify: `/Users/sanman/.claude/projects/-Users-sanman-Documents-my-stats/memory/MEMORY.md`

**Interfaces:** none (docs only).

- [ ] **Step 1: Update CLAUDE.md**

Update the counts and lists so logistic is the sixth guided analysis. Concretely:
- The "Architecture" paragraph: change "All five guided analyses" / "five analyses" phrasing to six, and add logistic to the enumerations of `web/guided/*` config files and `data-raw/*-demo-generator.R` files.
- The dispatcher description: add `logistic` to the list of figures that also return `code`.
- The "Lazy package install" paragraph: note `fig_logistic` (`R/logistic.R`) is univariable + multivariable logistic regression (OR Table 3 + adjusted-OR forest plot) using base `stats::glm` — **no `EXTRA_PACKAGES` entry** — with non-blocking guards (separation detection, EPV advisory, C-statistic, VIF, Cook's distance). Note Firth (`brglm2`) and Box-Tidwell logit-linearity are deferred fast-follows.

Exact edit (adjust surrounding wording to match the current file):

```
**Input.** All six guided analyses take an **uploaded CSV** parsed in-browser.
```

and add to the guided-analysis list:

```
… and Cox's (`web/guided/cox/guided-cox.js`), and Logistic's (`web/guided/logistic/guided-logistic.js`) …
```

- [ ] **Step 2: Write the memory note**

Create `.../memory/session-2026-07-17-logistic-regression.md`:

```markdown
---
name: session-2026-07-17-logistic-regression
description: 6th guided analysis — logistic regression (OR Table 3 + forest + base-R guards); research-backed design decisions and deferred items
metadata:
  type: project
---

Sixth guided analysis: **logistic regression** (binary-outcome "Table 3"), built as a near-clone of Cox ([[session-2026-07-17-cox-regression]]). Univariable + multivariable ORs side by side, adjusted-OR forest plot, downloadable R script — all base `stats::glm`, **no new package** (no broom.helpers/pROC/logistf/car).

Research-backed design decisions (deep-research + 3 focused agents, verified against the live r-wasm PACKAGES index):
- **CIs are Wald** (`exp(est±1.96·se)`), not profile — faster in webR, stable under separation.
- **Separation**: detect base-R (glm's "fitted probabilities 0/1" warning + OR>1e6) and WARN; do not auto-switch or refuse. `logistf` is a TRAP on webR (pulls mice+glmnet+mgcv, megabytes); if Firth is ever added, use `brglm2` (light tree) — deferred.
- **EPV ~10** is a SOFT advisory (min-class events / non-ref terms), never a gate (Vittinghoff/McCulloch relax it; van Smeden: evidence weak).
- **AUC/C-statistic** computed base-R (normalized Mann-Whitney via `rank()`); shown as one overall line, not per-covariate. pROC costs ~3.3MB (Rcpp+plyr) — skipped.
- **VIF** base-R (`1/(1-R²)` from `lm`), continuous predictors ONLY (factor GVIF not done); **Cook's distance** `>4/n` flags influential rows.
- **Continuous per-increment rescaling** (e.g. per 10 years): divide the column by k in prep; changes OR/CI but not p (Wald scale-invariance). Demo ships age per 10y.

Deferred fast-follows: Firth via brglm2; Box-Tidwell / natural-spline logit-linearity check; Hosmer-Lemeshow (deprecated — omitted).

Plan: `docs/superpowers/plans/2026-07-17-logistic-regression.md`.
```

- [ ] **Step 3: Add the MEMORY.md pointer**

Append to `.../memory/MEMORY.md`:

```
- [Session 2026-07-17: Logistic regression](session-2026-07-17-logistic-regression.md) — 6th guided analysis (OR Table 3 + forest + base-R guards); Wald CIs, base-R separation/EPV/AUC/VIF, no new pkg; deferred Firth/brglm2 + Box-Tidwell
```

- [ ] **Step 4: Final full-suite verification**

Run:
```bash
Rscript -e 'devtools::test()'
npm run test:unit
rm -rf web/R && cp -R R web/R && npm run test:e2e
```
Expected: R suite `[ FAIL 0 | WARN 0 | ... ]`; JS unit all-pass; e2e all-pass (slow first run).

- [ ] **Step 5: Commit**

```bash
git add CLAUDE.md
git commit -m "docs(logistic): reflect logistic regression as the sixth guided analysis"
```

---

## Self-Review

**Spec coverage (against the research decisions):**
- Table 3 (unadjusted+adjusted OR, CI, p, reference rows) → Task 1. ✓
- Adjusted-OR forest plot (null line at OR=1) → Task 2. ✓
- Continuous per-increment rescaling (p-invariant) → Task 1 (prep) + surfaced in form Task 8, demo Task 9. ✓
- Separation detection + warn (base-R, no Firth) → Task 3. ✓
- EPV soft advisory → Task 3. ✓
- C-statistic base-R, optional/overall → Task 3 (methods line) + e2e assert Task 11. ✓
- VIF (continuous only) + Cook's distance → Task 3. ✓
- Downloadable R script (glm, deparsed) → Task 4. ✓
- No new package / no EXTRA_PACKAGES → Global Constraints + Task 5. ✓
- Deferred (Firth/brglm2, Box-Tidwell, Hosmer-Lemeshow) → recorded in Task 12 memory + CLAUDE.md, not built. ✓
- Five-parallel-keys sync (R, dispatch, worker, app+html, package) → Tasks 1,5,10. ✓

**Placeholder scan:** Task 11 Step 1 references "mirror the Cox e2e picker interaction" — this is a genuine dependency on shared column-picker markup the implementer must read, not a code placeholder; the surrounding test is complete. No TBD/TODO/"add error handling" left.

**Type consistency:** `.logistic_prep` returns `list(df, covs, cov_types, ref_levels, incr, n, n_event, n_min, n_dropped)` — consumed with those exact names in Tasks 2-4. `.logistic_or_cell(smat, key)` signature stable across Tasks 1/3. Spec shape `{roles:{outcome, covariates}, options:{event_value, ref_levels, increments, source_filename, source_roles}}` identical across `buildLogisticSpec` (Task 7), demo spec (Task 9), R prep (Task 1). `.y` is the internal outcome column name in both R (`cols$.y`, `df$.y`) and the generated script (`dat$.y`). Nav label `Logistic regression` and figure key `logistic` consistent across Tasks 5/9/10.

One intentional cross-task detail: Task 1 Step 4 temporarily relaxes the `nzchar(out$code)` assertion (code is `""` until Task 4), and Task 4 Step 1 restores it. This is called out in both places.

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-07-17-logistic-regression.md`. Two execution options:**

**1. Subagent-Driven (recommended)** — I dispatch a fresh subagent per task, review between tasks, fast iteration.

**2. Inline Execution** — Execute tasks in this session using executing-plans, batch execution with checkpoints.

**Which approach?**
