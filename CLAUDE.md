# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

A free, static, **backend-less** clinical-figures + statistics tool for journal manuscripts. Real R packages (ggplot2, survival, cowplot, …) run **client-side in the browser via webR (WebAssembly)** — there is no server, and user data never leaves the browser. The R code is authored and tested as a normal R package (`manuscriptfigures`, see `DESCRIPTION`); webR is only the delivery vehicle. The static site lives in `web/` and is published to GitHub Pages.

## Commands

R is the source of statistical truth and is tested in plain R (no browser needed):

- Full R suite: `Rscript -e 'devtools::test()'`
- One file: `Rscript -e 'devtools::test(filter = "km")'` (matches `tests/testthat/test-km.R`)
- **Always use `devtools::test()`, never `testthat::test_file()`** — `test_file` silently swallows ggplot2 soft-deprecation warnings. **WARN 0 is a hard gate** (`[ FAIL 0 | WARN 0 | ... ]`); a leaked warning must be fixed at the source (ggplot2 4.x: use `linewidth`, not `size`/`label.size`; `geom_errorbar(orientation="y")`, not `geom_errorbarh`). If a warning genuinely originates inside a library's internals, wrap **only that library call** in `suppressWarnings()` — never wrap your own model fitting or data prep.

JS unit tests (plain Node, no framework) and browser end-to-end tests:

- JS unit: `npm run test:unit` — **a hand-maintained `node a.mjs && node b.mjs && …` chain in `package.json`, not a glob.** Tests live in `web/lib/` and `web/guided/**/`; a new `*.test.mjs` that is not appended to that chain silently never runs. Always add the new file to `test:unit` in the same commit that creates it.
- E2E (Playwright + webR): `rm -rf web/R && cp -R R web/R && npm run test:e2e`
- Serve locally: `rm -rf web/R && cp -R R web/R && npm run serve` (→ http://localhost:8321; non-default port so stale service workers from other localhost PWA projects can't hijack the page)

**`web/R/` is a gitignored build copy of `R/`.** The worker fetches R sources at `R/<file>` relative to the `web/` root, so `R/` must be copied into `web/` before serving or running e2e. Use `rm -rf web/R` first — a bare `cp -R R web/R` nests into an existing dir. The deploy workflow does this copy at publish time.

## Architecture

**One contract, one dispatcher.** `web/app.js` sends a JSON spec to a webR session in a Web Worker (`web/worker.js`); R's `render_figure(json_string)` (`R/dispatch.R`) routes on `spec$figure` to one `fig_<name>(spec)` per figure, each returning `list(svg = <character>, text = <character>)`. Summary, KM, Group comparison, Cox, and Logistic also return `code` — a complete runnable R script whose statistical calls are deparsed from the exact expressions the app evaluated (shared builders in `R/script.R`); the UI serves it via the Console toolbar's `.R` button, reading `#stats`'s `data-r-code` at click time. Specs carry `options.source_filename` for user uploads; demo specs omit it, which makes the script embed the example data. `render_figure` is wrapped in `tryCatch` and **never throws** — errors come back as `{"ok":false,"error":...}`. `app.js` injects `out.svg` into `#preview` (as `innerHTML`) and `out.text` into `#stats`.

- **`text`** is a copy-pasteable methods/results sentence (or TSV for tables).
- **`svg`** normally holds an `<svg>`, but the table analyses — `fig_summary` (distribution plots), `fig_cox` (HR forest), `fig_logistic` (OR forest) — put an **HTML `<table>`** (plus the sibling plot SVGs) in the `svg` field instead — the UI injects it the same way, so tables render with no special-casing.

**Input.** All six guided analyses take an **uploaded CSV** parsed in-browser. Summary statistics (`summary`) is auto-computed and normality-aware: the user uploads a CSV, ticks a variable checklist, and R decides mean ± SD vs median (IQR) per variable and builds the table + distribution plots — no typed numbers. The shared foundation lives in `web/lib/`: `csv.js` (`parseCsv` → `{columns, rows, types}` with numeric/categorical inference, plus `toCsv`, its guarded inverse), `columnpicker.js` (`renderColumnPicker` → type-filtered dropdowns mapping columns to analysis roles), and `modelform.js` (`retainedSelection`, `reconcileRefLevels`, `renderReadiness`, `countDroppedRows` — the decision logic the Cox and logistic analyze forms share; change it there, never in one form). Forms send `{figure, data:[...rows], roles:{...}, options:{...}}`; R extracts columns by role. (All six analyses build on the shared `csv.js` foundation; KM's analyze form maps arbitrary columns to time/status/group roles with a user-confirmed event value in `web/guided/km/{analyze-form,spec}.js`, Cox's maps time/status plus a covariate **multi-select** with a per-categorical-covariate reference-level dropdown in `web/guided/cox/{analyze-form,spec}.js`, and Logistic's maps a binary outcome with a user-confirmed event value plus the same covariate multi-select — categorical covariates still get the reference-level dropdown — adding a **per-numeric-covariate increment** box so a continuous covariate's odds ratio is reported per a clinically meaningful step (e.g. age per 10 years) rather than per single unit, in `web/guided/logistic/{analyze-form,spec}.js`.)

Kaplan–Meier, Table 1 (Summary), Explore plot, Group comparison, Cox regression, **and** logistic regression all route through a guided three-stage shell — Understand / Try an Example / Analyze Your Data — built by the shared `createGuidedShell(config)` factory in `web/guided/shell.js`, with session semantics in `web/guided/session-state.js`. A new guided analysis is a **config file** — the `createGuidedShell({...})` config object lives in `web/guided/guided-analysis.js` (KM), `web/guided/summary/guided-summary.js` (Summary), `web/guided/explore/guided-explore.js` (Explore), `web/guided/groupcompare/guided-groupcompare.js` (Group comparison), `web/guided/cox/guided-cox.js` (Cox), and `web/guided/logistic/guided-logistic.js` (Logistic), with each analysis's content/demo pieces beside it in `web/guided/km/`, `web/guided/summary/`, `web/guided/explore/`, `web/guided/groupcompare/`, `web/guided/cox/`, and `web/guided/logistic/` — never a copy of the shell. All ship frozen synthetic demos (`data-raw/km-demo-generator.R`, `data-raw/summary-demo-generator.R`, `data-raw/explore-demo-generator.R`, `data-raw/groupcompare-demo-generator.R`, `data-raw/cox-demo-generator.R`, `data-raw/logistic-demo-generator.R`). Explore (`R/explore.R`) builds its ggplot as an **expression** and `deparse`s the same expression into the code pane — the R shown to the user is never string-built, it is the exact call that ran. Group comparison (`R/groupcompare.R`) routes on outcome type (numeric → t/ANOVA family, categorical → chi-square/Fisher), auto-selects parametric vs non-parametric by reusing `.summary_decide`, and reports an effect size + 95% CI + p-value with Tukey/Dunn post-hoc for 3+ groups — all base-R, no `EXTRA_PACKAGES` entry. `createGuidedShell` supports `liveRender: true` (coalesced one-in-flight rendering with a busy overlay, used by Explore's live demo/analyze builders), and `web/lib/export.js`'s `textExportDescriptor` makes the text toolbar `.R`-aware on Explore (a downloadable ggplot2 script) while other analyses stay `.tsv`. The form registry in `web/app.js` still routes nav clicks and is the extension point for future analyses.

**Lazy package install.** The worker boots with only the shared packages (`ggplot2, svglite, jsonlite`). Heavy per-figure packages install on first use via `EXTRA_PACKAGES` in `worker.js` (`km → survival/cowplot`, `cox → survival`), guarded by a single-flight promise so concurrent requests install once. This keeps a Summary-statistics user from downloading the survival tree; a KM user who then opens Cox pays nothing extra (survival already resident). `fig_summary` (`R/summarize.R`), `fig_explore` (`R/explore.R`), `fig_groupcompare` (`R/groupcompare.R`), and `fig_logistic` (`R/logistic.R`) use only base `stats` + `ggplot2`, so they need **no** `EXTRA_PACKAGES` entry. `fig_cox` (`R/cox.R`) is univariable + multivariable Cox regression: a Table-3 HR table (unadjusted vs adjusted) + an adjusted-HR forest plot, with the PH assumption checked via `cox.zph` and reported non-blocking.

`fig_logistic` (`R/logistic.R`) is the binary-outcome twin: univariable + multivariable logistic regression via base `stats::glm` — a Table-3 OR table (unadjusted vs adjusted) + an adjusted-OR forest plot (log axis, dashed null line at OR = 1) — with **no new package and no `DESCRIPTION` import**. CIs are Wald (`exp(est ± 1.96·se)`); a cell reads `"not reliably estimated"` when the estimate is non-finite or the upper bound blows past OR 1e6. Continuous covariates support **per-increment rescaling** (divide the column by k in prep, e.g. "per 10 years") — the OR and CI change, the p-value does not. Every diagnostic is appended to the methods text and **none gates a fit**: a separation-or-severe-collinearity CAUTION, an EPV < 10 advisory, an overall C-statistic (base-R normalized Mann-Whitney via `rank()`), a VIF > 5 note for continuous predictors only (`1/(1-R²)` from `lm`), and a Cook's-distance > 4/n influential-points note. Deferred by design: Firth via `brglm2` (never `logistf` — it drags in mice + glmnet + mgcv on webR) and a Box-Tidwell / spline logit-linearity check; Hosmer–Lemeshow was considered and omitted as deprecated.

**Journal export.** Every rendered output downloads for journal submission via the pane-header toolbars (`web/export-ui.js` + `web/lib/export.js`): PNG at 300/600/1200 dpi with the true DPI stamped into the file's `pHYs` chunk (unstamped canvas PNGs read as 72 dpi in journal submission checkers), stitched or per-panel SVG, and copy/`.tsv` of the table + methods text (journals want tables as editable text). Handlers read `#preview`/`#stats` at click time; a MutationObserver drives enabled state; `app.js` clears both panes on nav switch so a stale figure can never export under another analysis's filename. All exports are client-side Blob URLs — the no-egress invariant applies. (The pre-trim 8-analysis app survives at git tag `pre-trim-8-analyses`.)

**No data egress.** The only network calls are the webR runtime/packages from the CDN and `fetch("R/*.R")`. There is no analytics or upload of user data — this is an invariant, not a preference. All statistics live in R; JS only parses CSVs and marshals columns. Fonts (IBM Plex) are self-hosted in `web/fonts/` for the same reason — never link a font CDN.

**UI tokens.** All chrome styling routes through the CSS variables at the top of `web/styles.css` — warm paper surfaces (`--chrome` desk, `--panel`, `--rail`, `--output-canvas`, `--sheet`) over graphite ink, teal `--accent` `#0d6b63`, `--radius*`, warn palette. Three fonts, all self-hosted: IBM Plex Sans/Mono for chrome, **Source Serif 4 for the rendered artifact only** (table row labels, italic captions, teaching-prose headings, the wordmark). A rendered table is a "galley proof" — R already wraps every table analysis in `.summary-output > .table-scroll`, and that wrapper is the sheet; the journal three-rule borders ride on the **cells**, not the table, so the paper margin stays outside them. `--ink-faint` is decorative only (3.6:1); anything under 14px uses `--ink-muted`. Data colors in figures (Tol palette, `R/km.R .km_palette`) are deliberately distinct from chrome colors. **Mobile is real, not theoretical** — Summary and Kaplan–Meier (the heaviest path, `survival` + `cowplot` on top of the runtime) were both verified running in a phone browser on 2026-07-20, and the figures downloaded — so the whole workflow (load, fit, render, export) is phone-viable and webR should not be assumed desktop-only. Two constraints follow: form controls take **16px under `pointer: coarse`** (iOS Safari zooms the page when focusing anything smaller and never restores it, trapping every dropdown in the analyze forms), and the export toolbar's text hint hides below the 960px breakpoint where it wrapped into the dpi select. Known UI/a11y fast-follows live in `.scratch/guided-analysis-km/issues/stage-a-fast-follows.md`.

## Adding a figure or analysis

A new figure `foo` requires **five parallel keys to stay in sync**, or it will pass R tests but 404 / mis-route in the browser:

1. `R/foo.R` — `fig_foo(spec)` returning `list(svg=, text=)`. Reuse `.svg_string(plot, w, h)` and `%||%` — both live in `R/dispatch.R` — do **not** redefine them.
2. `R/dispatch.R` — add `foo = fig_foo(spec),` to the `switch`.
3. `web/worker.js` — add `"foo.R"` to the boot-time R-file fetch loop (every new R file needs this), and add a `foo: [...]` entry to `EXTRA_PACKAGES` **only** if it needs packages beyond the boot set.
4. `web/guided/foo/guided-foo.js` — a `createGuidedShell({...})` config exporting `renderGuidedFoo`, with the analysis's Understand/demo/analyze-form pieces beside it in `web/guided/foo/`; `web/app.js` — import `renderGuidedFoo` and add `foo: renderGuidedFoo` to the `forms` registry; `web/index.html` — a `<button data-figure="foo">`.
5. If it uses a new R package, add it to `DESCRIPTION` Imports (CI installs deps via `local::.`) **and** to the worker's `EXTRA_PACKAGES` for that figure.

**`R/*.R` is network-first in `web/sw.js`; everything else same-origin is stale-while-revalidate.** R sources are the statistical source of truth, so a cached copy may never answer while the network is reachable — the cache exists for them only as an offline fallback. This used to be SWR like the rest of the shell, and it bit twice, both times reading as "the fix didn't ship": a returning user's *first* load after a deploy computed with the previous R sources. The routing decision is the pure `routeFor(req)` and is pinned by `web/sw.test.mjs`, which runs the real `sw.js` in a vm sandbox. Bumping `CACHE` on an `R/` change is now a convenience (a hard reset lever), not the guard standing between a deploy and wrong output — still bump it when changing the precached shell.

For CSV analyses, build the form on `web/lib/csv.js` + `web/lib/columnpicker.js`; coerce spec columns to numeric with a suppress-warning-then-clear-`stop()` pattern (see `.numeric_col` in `R/summarize.R`) so a non-numeric column yields a readable error, not a leaked coercion warning.

## Environment notes

- Local R is Homebrew R 4.6 (bleeding-edge → CRAN builds packages from source). See the memory note `r-toolchain-homebrew-webr-gotchas` for the system-lib / Anaconda-PATH fixes needed to install the package tree locally.
- CI (`.github/workflows/ci.yml`) runs the R suite and the JS unit tests, but **not** the Playwright e2e (it downloads webR — slow/flaky). Deploy (`deploy.yml`) copies `R/`→`web/R/` and publishes `web/` to Pages on push to `main`.
- **The site serves from the custom domain `figurastats.org`** (Cloudflare registrar + DNS, GitHub Pages origin). `web/CNAME` holds it — it must stay in `web/`, never the repo root, because `deploy.yml` uploads `path: web` as the artifact; a root-level `CNAME` is not published and every deploy silently clears the custom domain. GitHub's repo-level Pages setting must name the same domain. The same trap applies to **`web/preview.png`**, the Open Graph link-preview image referenced absolutely from `index.html`'s `og:image` — at the repo root it would 404 and produce a card with no image. Its source is `data-raw/og-card.html` (beside the demo generators); it pulls `../web/styles.css` so the card cannot drift from the shipped tokens, and regenerates with `browse goto file://./data-raw/og-card.html` then `browse screenshot web/preview.png --selector ".card"`.
- **Cloudflare Email Routing is enabled** (since 2026-07-22): `feedback@figurastats.org` forwards to the owner's Gmail (verified working). Enabling it swapped the domain's null-MX + `SPF -all` (a deliberate no-mail lockdown) for Cloudflare's MX + `SPF ~all` + a `cf2024-1` DKIM record; **DMARC `p=reject` was intentionally kept**, so anti-spoofing still enforces. Mail-only DNS — the website's A/proxy records were untouched. The rail's feedback affordance (`.fb-copy` in `web/index.html` nav-pane foot + `styles.css`, copy handler in `app.js`) shows this address as **plain click-to-copy text, not a mailto** — a mailto silently dead-ends for webmail-only users with no default mail app.
- **Cloudflare's proxy IS enabled** (since 2026-07-21), so an edge cache now sits in front of the origin — a third cache that neither the service worker's network-first nor its `cache: "reload"` can reach. Two things hold it safe and must not be undone: a **Cache Rule bypassing `/R/*`** (verified: `cf-cache-status: DYNAMIC` on `R/cox.R`) and **SSL/TLS mode Full** (Flexible loops against Pages' HTTP→HTTPS redirect; verified: HTTP→HTTPS resolves in exactly one redirect). See the THIRD-CACHE WARNING in `web/sw.js`. GitHub Pages holds its own Let's Encrypt cert for the apex, so both hops are certificated.

## Design docs

Specs and implementation plans live in `docs/superpowers/{specs,plans}/`. Read the relevant one before extending a feature — they carry the rationale (e.g. why the KM figure ships a built-in number-at-risk table via cowplot, why the app was trimmed to guided analyses only).

## Agent skills

### Issue tracker

Issues are tracked as local Markdown files under `.scratch/`; external pull requests are not a request surface. See `docs/agents/issue-tracker.md`.

All ten issues in `.scratch/logistic-regression/issues/` are **resolved** as of 2026-07-20 — the cox backtick keys, the analyze-form state loss, the shell's control-lock defeat, the script's reference level, the forest reliability filter, the dropped-row preview, the cox/logistic form divergence, the forest axis's lower tail, the cox table/plot disagreement, and the service worker's stale-`R/` window. Read an issue file before re-reporting anything from it; each carries its fix in a Comments section.

One gap is knowingly left open and has no issue yet: cox does not reject a status column that is also selected as a covariate, where logistic rejects the analogous case. Closing it changes behavior.

### Triage labels

Uses the standard `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix` vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository using root `CONTEXT.md` and `docs/adr/`. See `docs/agents/domain.md`.
